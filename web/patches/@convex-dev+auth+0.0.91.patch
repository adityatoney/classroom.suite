diff --git a/node_modules/@convex-dev/auth/dist/server/cookies.js b/node_modules/@convex-dev/auth/dist/server/cookies.js
index b76dd05..0d1b2a7 100644
--- a/node_modules/@convex-dev/auth/dist/server/cookies.js
+++ b/node_modules/@convex-dev/auth/dist/server/cookies.js
@@ -1,10 +1,14 @@
 import { isLocalHost } from "./utils.js";
+const _localUrl = process.env.CONVEX_SITE_URL
+    ?? process.env.CONVEX_SITE_ORIGIN
+    ?? process.env.SITE_URL;
+const _isLocal = isLocalHost(_localUrl);
 export const SHARED_COOKIE_OPTIONS = {
     httpOnly: true,
-    sameSite: "none",
-    secure: true,
+    sameSite: _isLocal ? "lax" : "none",
+    secure: !_isLocal,
     path: "/",
-    partitioned: true,
+    ...(!_isLocal && { partitioned: true }),
 };
 const REDIRECT_MAX_AGE = 60 * 15; // 15 minutes in seconds
 export function redirectToParamCookie(providerId, redirectTo) {
@@ -29,6 +33,6 @@ export function useRedirectToParam(providerId, cookies) {
     return { redirectTo, updatedCookie };
 }
 function redirectToParamCookieName(providerId) {
-    return (!isLocalHost(process.env.CONVEX_SITE_URL) ? "__Host-" : "") + providerId + "RedirectTo";
+    return (!_isLocal ? "__Host-" : "") + providerId + "RedirectTo";
 }
 //# sourceMappingURL=cookies.js.map
\ No newline at end of file
